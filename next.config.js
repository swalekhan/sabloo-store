/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['d2k503pumj0lc5.cloudfront.net'],
    disableStaticImages: true,
  },
}

module.exports = nextConfig
